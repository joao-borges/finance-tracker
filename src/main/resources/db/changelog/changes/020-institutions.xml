<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Institutions: a grouping layer above accounts (the bank), with an
         off-budget toggle that cascades to all of the institution's accounts. -->
    <changeSet id="020-institutions" author="joao">
        <createTable tableName="institutions">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_institutions"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_institutions_name"/>
            </column>
            <column name="website" type="varchar(512)"/>
            <column name="logo_url" type="varchar(1024)"/>
            <column name="off_budget" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addColumn tableName="accounts">
            <column name="institution_id" type="bigint"/>
        </addColumn>
        <addForeignKeyConstraint baseTableName="accounts" baseColumnNames="institution_id"
                                 constraintName="fk_accounts_institution"
                                 referencedTableName="institutions" referencedColumnNames="id"
                                 onDelete="SET NULL"/>
    </changeSet>

</databaseChangeLog>
